var should = require('should')
  , neo4jGraph = require('./boot')
  , Builder = require('../src/Builder');

describe('Test match functionality', function() {
  "use strict";

  it('Test the match method only with the run method', function() {
//    var builder = neo4jGraph.Builder;
//
//    should(builder).be.not.null().and.be.instanceof(Builder);
//
//    builder
//      .reset()
//      .Match('node', 'Node', {field1: 34, field2: "Hello, worked!"});
//
//    neo4jGraph.run(builder, function(err, results) {
//      should(err).be.null();
//      should(results).be.an.Array().and.has.lengthOf(1);
//
//      var node = results[0].node;
//    });
  });
});